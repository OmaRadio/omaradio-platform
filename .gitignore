# Python
__pycache__/
*.py[oc]
.venv/

# Secrets
.env

# Kokoro model weights -- ~350MB, fetched separately (see pipeline/dj-segment/README.md)
*.onnx
*.bin

# Generated audio -- pipeline output, not source
pipeline/**/*.mp3
pipeline/**/*.wav

# Scheduler DB -- generated locally/on transmitter-one via scheduler/db/migrate.py,
# not committed (real data, differs per machine, would just be binary noise in git)
scheduler/db/*.sqlite3
